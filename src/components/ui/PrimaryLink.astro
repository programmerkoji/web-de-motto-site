---
interface Props {
  href: string;
  variant?: 'primary' | 'secondary';
}

const { href, variant = 'primary' } = Astro.props;
const className = variant === 'primary' ? 'primary-link' : 'secondary-link';
---

<a href={href} class={className}>
  <slot />
</a>
